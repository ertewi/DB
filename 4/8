Вывести из таблицы заказов уникальные номера продавцов
и показатель активности каждого из них на основе количества заказов:
«активный» — 4+ заказа,
«умеренный» — 2-3 заказа,
«неактивный» — 1 заказ.

select distinct snum, count(*),
                CASE
                    WHEN count(*)>4 THEN '«активный»'
                    WHEN count(*)>=2 and count(*)<=3 THEN '«умеренный»'
                    WHEN count(*)<2 THEN '«неактивный»'
                    END as active
from ord
group by snum